# v0.2 Known Limits & Tier 3 Close-Out

This document records **accepted v0.2 local-dev limitations** and the **Tier 3 work order** for closing v0.2. Tier 1 (monolith KYC bridge + E2E) and Tier 2 mobile (auth refresh, routing, verify-email, resumability) are complete on branch `feature/v0.2-tier2-mobile-auth-routing`.

---

## Tier 2 status (mobile — done)

| Item | Status |
|------|--------|
| Bearer token + 401 refresh interceptor | Done (`mobile/src/api/client.ts`) |
| Post-login routing by `kyc_status` | Done (`resolvePostAuthNavigation.ts`, `LoginScreen`) |
| Cold-start routing | Done (`AuthenticatedBootstrapScreen`) |
| Verify-email deep link (`token` param) | Done |
| KYC resumability on Home | Done (`useKycResumability`) |
| Upload slot restore from `uploadedTypes` | Done |

**Remaining Tier 2 (platform/API — defer to Tier 3 pass):**

- 422 vs 400 consistency (`GlobalExceptionHandler` returns 400; some OpenAPI annotations say 422)
- Issue Plan ↔ Schema doc reconciliation (extra KYC tables/fields)
- Confirm `/api/v1/kyc/submissions/me` is canonical (mobile uses `/me`)

---

## Tier 3 — Accepted for v0.2 local dev

These are **intentional stubs** for v0.2. Production implementations are tracked for v0.5+ unless noted.

| Item | Location | v0.2 decision | Follow-up |
|------|----------|---------------|-----------|
| Supabase HMAC verifier stub | `kyc-service/.../LocalSignatureVerifier.java` | Accept for local dev | Real `SupabaseSignatureVerifier` when Supabase creds exist |
| Signed upload URLs | `LocalFilesystemObjectStorage` | Accept; mobile PUT + confirm path is what we test | Real Supabase signed URLs in prod |
| Storage `delete()` stub | `LocalFilesystemObjectStorage.delete()` | Accept; logs only | `SupabaseObjectStorage.delete()` |
| Admin doc preview URLs | Admin console localhost view | Accept broken `<img>` on local URLs | Signed view URLs with Supabase |
| Ghana Card masking | Client-only in admin/mobile | Accept if documented | Optional server-side redaction |
| `[P0_ALERT]` | Log-only alerts | Accept | Wire PagerDuty/Slack in v0.5 |
| `ADMIN_FORCE` revoke reason | `ResetPasswordService` | Accept + commented | Optional enum value later |
| `SecurityConfig` from v0.2-015 | Monolith | Accept; extend, don't re-scaffold | — |
| `expo-camera` unused | `mobile/package.json` | **Remove** or use in KYC capture (pick one in Tier 3) | Avoid dead dependency |
| Contact support no-op | Mobile/admin placeholders | Wire `mailto:` or hide until v0.3 | Product decision |
| `RESUBMISSION_REQUIRED` null reason | KYC rejection flow | Document; no UI path yet | Implement when product needs it |
| Admin → KYC direct `:8082` | Admin console env | Accept; document architecture | Mobile uses monolith proxy (`:8080`) |

---

## Tier 3 — Recommended work order

Execute on branch `feature/v0.2-tier3-closeout` after Tier 2 is merged and CI is green.

### 1. Documentation & infra hygiene (low risk)

- [ ] Add local dev runbook to root `README.md` (ports, RabbitMQ recreate, payments-db)
- [ ] File GitHub issues for each accepted stub with `v0.5` milestone
- [ ] Reconcile OpenAPI `@ApiResponse(422)` → `400` where handler uses `BAD_REQUEST`

### 2. Schema / Issue Plan reconciliation

- [ ] Update Schema doc §8 **or** Issue Plan prose for: `provider_decisions`, `manual_review_queue`, `processed_document_events`, deletion job design
- [ ] Align event javadoc `VERIFIED` → `APPROVED` where still stale

### 3. Small code cleanups

- [ ] Remove `expo-camera` from mobile **or** switch document capture to camera API
- [ ] Hide or wire “Contact support” buttons
- [ ] Add `@Deprecated`/comment on any duplicate KYC status endpoints if `/{id}` exists alongside `/me`

### 4. Merge & close v0.2

- [ ] Merge `feature/v0.2-tier2-mobile-auth-routing` → `develop`
- [ ] Merge Tier 3 doc/cleanup PR → `develop`
- [ ] Re-run `scripts/tier1-e2e-smoke.ps1` on clean stack
- [ ] Tag or milestone-close v0.2 when Tier 3 decisions are written and CI is green

---

## Local development quick reference

| Service | Host port | Notes |
|---------|-----------|-------|
| Monolith | 8080 | Mobile API base URL; KYC proxy |
| KYC service | 8082 | Admin console direct access |
| Monolith DB | 5436 | |
| KYC DB | 5434 | |
| Payments DB | **15433** | Host port (not 5433 — Windows conflict). May fall in Hyper-V excluded ranges on some machines; change in `infra/compose/databases.yml` + `payments-service/application.yml` if bind fails |
| RabbitMQ | 5672 / 15672 | Management UI |
| Mailpit | 8025 | Email capture |

**RabbitMQ topology changes:** queue/exchange updates in `infra/docker/rabbitmq/definitions.json` require a volume reset:

```powershell
docker compose down -v
docker compose up --build -d
```

---

## Architecture note: mobile vs admin KYC routing

- **Mobile** → `EXPO_PUBLIC_API_BASE_URL` (monolith `:8080`) → `KycProxyController` → kyc-service
- **Admin console** → `VITE_KYC_API_BASE_URL` (kyc-service `:8082`) directly

Both are valid for v0.2; do not point mobile at `:8082` without also sending monolith JWT validation through a shared auth layer.

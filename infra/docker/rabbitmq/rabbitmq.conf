# infra/docker/rabbitmq/rabbitmq.conf

# Default virtual host for Stash
default_vhost = stash

# Default user is configured via environment variables (RABBITMQ_DEFAULT_USER / PASS)
# These are set in docker-compose, not here, so credentials stay out of committed config.

# Management plugin is enabled via the image tag (rabbitmq:3.13-management)
# This file just tunes its behaviour.
management.load_definitions = /etc/rabbitmq/definitions.json
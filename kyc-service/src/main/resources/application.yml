spring:
  application:
    name: kyc-service
  datasource:
    url: jdbc:postgresql://localhost:5432/stash_kyc
    username: stash
    password: stash
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
    baseline-on-migrate: true

server:
  port: 8082

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: when-authorized

# Shared defaults for @SpringBootTest — DynamicPropertySource overrides DB and AES key.
spring:
  rabbitmq:
    listener:
      simple:
        auto-startup: false

stash:
  kyc:
    base-url: http://localhost:8082
    ghana-card-encryption-key: dGVzdC1hZXMtMjU2LWtleS0zMi1ieXRlcy1sb25nISE=
    storage:
      bucket: kyc-documents
      provider: local

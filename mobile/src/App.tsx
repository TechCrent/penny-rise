import React from 'react';
import HomeScreen from './screens/HomeScreen';

/**
 * Root component. Navigation, providers, and theme will be
 * wired in here in later milestones.
 */
export default function App() {
  return <HomeScreen />;
}

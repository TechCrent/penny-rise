# Local development overrides.
# Activated by: SPRING_PROFILES_ACTIVE=local
# All sensitive values come from environment variables — never hardcode here.

spring:
  jpa:
    show-sql: true

stash:
  email:
    provider: smtp
    from-address: noreply@stash.local
    from-name: Stash (Local)
    base-url: http://localhost:8080
  security:
    jwt:
      access-token-expiry-minutes: 15
    bcrypt-cost: 12
